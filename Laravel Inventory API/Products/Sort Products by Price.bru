meta {
  name: Sort Products by Price
  type: http
  seq: 6
}

get {
  url: {{base_url}}/api/products?sort_by=price&sort_order=asc
  body: none
  auth: none
}

params:query {
  sort_by: price
  sort_order: asc
}

headers {
  Authorization: Bearer {{auth_token}}
}

docs {
  Trier les produits par prix croissant
}
